"use strict";

/// Handler for `/dcode/v1/{pageID}` resource

/**
 * 
 * @param {*} req 
 * @param {*} res 
 * @param {*} next 
 */
function pageHandler(req, res, next) {
    // @TODO: Implement page handler
}

module.exports = {
    pageHandler
}