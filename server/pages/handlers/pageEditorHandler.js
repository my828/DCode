"use strict";

/// Handler for `/dcode/v1/{pageID}/editor` resource

function pageEditorHandler(req, res, next) {
    // @TODO: Implement page editor handler
}

module.exports = {
    pageEditorHandler
}